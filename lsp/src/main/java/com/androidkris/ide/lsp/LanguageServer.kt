package com.androidkris.ide.lsp

/**
 * Fase 5 home: editor intelligence via the Language Server Protocol.
 *
 * Will run AndroidIDE's Java LSP (Eclipse JDT fork), an XML LSP (LemMinX) for
 * layouts/manifest, and a Kotlin LSP, feeding completion / diagnostics / hover /
 * go-to-definition back into the sora-editor surface.
 *
 * Placeholder for Fase 0 so the module boundary is real.
 */
object LspModule {
    const val PHASE = "Fase 5 — LSP (Java/XML/Kotlin) intelligence"
}
