# Keep line numbers for stack traces.
-keepattributes SourceFile,LineNumberTable

# Hilt / Dagger generated code is handled by the plugin's consumer rules.
# Add module-specific keep rules (sora-editor, Termux, LSP) here as those land.
